const example = [-1, 4, 3, -2, 5];

const negindexValueMapping = {};
for(let i = 0; i < example.length; i++) {
    negindexValueMapping[i] = example[i];
} 

for(let key in negindexValueMapping) {
    
} 

console.log(negindexValueMapping);

